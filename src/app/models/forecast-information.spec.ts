import { ForecastInformation } from './forecast-information';

describe('ForecastInformation', () => {
  it('should create an instance', () => {
    expect(new ForecastInformation()).toBeTruthy();
  });
});
